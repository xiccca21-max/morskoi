import { useNavigate } from 'react-router-dom';
import { motion } from 'framer-motion';
import { Icon } from '../components/Icon';

interface Section {
  icon: 'anchor' | 'target' | 'swords' | 'flag' | 'skull' | 'shield' | 'scroll' | 'coins';
  title: string;
  items: string[];
}

const SECTIONS: Section[] = [
  {
    icon: 'anchor',
    title: 'Поле и флот',
    items: [
      'Поле 10×10, оси: буквы А–К (без Й/Ё) и цифры 1–10.',
      '1 линкор (4 клетки), 2 крейсера (по 3), 3 эсминца (по 2), 4 катера (по 1) — итого 10 кораблей.',
      'Корабли расставляются горизонтально или вертикально.',
      'Запрещено касание бортами и углами — между кораблями всегда минимум 1 клетка.',
      'На расстановку даётся 30 секунд. При истечении времени флот расставляется автоматически.',
    ],
  },
  {
    icon: 'target',
    title: 'Ход и выстрел',
    items: [
      'Первым ходит игрок, начавший матч (player1).',
      'За ход нужно нажать на клетку вражеского поля.',
      'Попал в корабль — получаешь ещё один выстрел. Ходишь, пока не промахнёшься.',
      'Промахнулся — ход переходит к сопернику.',
      'Нельзя стрелять в уже атакованную клетку.',
      'На каждый выстрел отведено 20 секунд (настраивается администратором).',
    ],
  },
  {
    icon: 'swords',
    title: 'Потопление корабля',
    items: [
      'Корабль потоплен, когда все его клетки поражены.',
      'После потопления клетки вокруг корабля автоматически отмечаются промахами — там нет других кораблей.',
      'Потопленный корабль отображается как обломки с дымом.',
      'Игра заканчивается, когда все 10 кораблей одного игрока потоплены.',
    ],
  },
  {
    icon: 'skull',
    title: 'AFK и дисконнект',
    items: [
      'Если ты не делаешь ход 3 раза подряд — засчитывается поражение, ставка уходит сопернику.',
      'Потеря связи не прерывает бой: при переподключении ты автоматически попадаешь обратно в бой.',
      'Если связь оборвалась у обоих — бой продолжится, как только кто-то переподключится.',
      'Нельзя закрыть приложение во время боя без последствий — засчитывается сдача.',
    ],
  },
  {
    icon: 'flag',
    title: 'Сдача',
    items: [
      'Ты можешь сдаться в любой момент через кнопку «Сдаться» (иконка флага) в бою.',
      'Перед сдачей система спросит подтверждение — выход без подтверждения невозможен.',
      'При сдаче вся ставка (твоя половина банка) уходит сопернику за вычетом комиссии.',
      'Сдача в фазе расстановки: ставка возвращается обоим, бой отменяется.',
    ],
  },
  {
    icon: 'coins',
    title: 'Деньги и комиссия',
    items: [
      'Ставка блокируется на балансе в момент начала боя — нельзя потратить эти средства в другом месте.',
      'Общий банк = ставка × 2. Победитель получает 95% банка.',
      '5% — комиссия платформы.',
      'Выплата происходит мгновенно после окончания боя.',
      'Если произошла ничья (технический сбой) — ставки возвращаются обоим без комиссии.',
    ],
  },
  {
    icon: 'shield',
    title: 'Честная игра',
    items: [
      'Расстановка кораблей не видна сопернику до конца боя.',
      'Каждое действие защищено одноразовым nonce — повторная отправка команды не принимается.',
      'Все результаты и транзакции хранятся в базе данных и не могут быть изменены задним числом.',
      'Подозрительная активность (ботовод, скрипты) ведёт к блокировке аккаунта.',
    ],
  },
];

export default function RulesScreen() {
  const navigate = useNavigate();
  return (
    <div className="max-w-md mx-auto space-y-5 pb-8">
      <button onClick={() => navigate(-1)} className="flex items-center gap-2 text-muted text-sm">
        <Icon name="arrow-right" size={14} className="rotate-180" /> Назад
      </button>

      <h1 className="title text-main text-xl">Правила игры</h1>
      <p className="text-muted text-sm">Полные правила морского боя и последствия нарушений.</p>

      {SECTIONS.map((sec, i) => (
        <motion.section
          key={sec.title}
          initial={{ opacity: 0, y: 10 }}
          animate={{ opacity: 1, y: 0 }}
          transition={{ delay: i * 0.06 }}
          className="card p-5"
        >
          <div className="flex items-center gap-2 mb-3">
            <Icon name={sec.icon} size={18} className="text-danger" />
            <p className="font-display text-main text-sm">{sec.title}</p>
          </div>
          <ul className="space-y-2">
            {sec.items.map((item, j) => (
              <li key={j} className="flex gap-2.5 text-xs text-main leading-relaxed">
                <span className="shrink-0 w-1.5 h-1.5 mt-1.5 rounded-full bg-danger" />
                {item}
              </li>
            ))}
          </ul>
        </motion.section>
      ))}

      <div className="card p-4 border-danger">
        <div className="flex gap-2 items-start">
          <Icon name="scroll" size={16} className="text-danger shrink-0 mt-0.5" />
          <p className="text-xs text-muted leading-relaxed">
            Регистрируясь и играя, ты соглашаешься с правилами. Игра предназначена для лиц старше 18 лет.
            Платформа не несёт ответственности за решения, принятые в состоянии аффекта.
          </p>
        </div>
      </div>
    </div>
  );
}
